<UserControl x:Class="クスリのナカヤマ薬局ツール.UserControls.シフト表.Components.DayAndDateCell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="60" Width="70">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Border BorderThickness="1" BorderBrush="Black">
                <TextBlock Text="{Binding Path= ShortTimeDate}" TextAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>

        <Grid Grid.Column="1">
            <Border BorderThickness="1" BorderBrush="Black" Margin="-1,0,0,0">
                <TextBlock Text="{Binding Path= DayOfWeek}" TextAlignment="Center" VerticalAlignment="Center"/>
            </Border>

        </Grid>

    </Grid>
</UserControl>
